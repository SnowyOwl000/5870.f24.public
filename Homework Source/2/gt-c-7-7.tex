\item\problemnumber{GT C}{7}{7}{-}{.}
Suppose we implement the tree-based union-find data structure using the
union-by-size heuristic and path-compression heuristics. Show that the total
running time for performing a sequence of $m$ union and find operations,
starting with $n$ singleton sets, is $O(m), if $m\ge n\lg n$.\\[12pt]
\ifanswers
\textcolor{blue}{
\textbf{Answer:}\\[6pt]
Answer goes here
}
\newpage
\fi
